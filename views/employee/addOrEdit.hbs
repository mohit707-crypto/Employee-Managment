<h3>{{viewTitle}}</h3>

<form action="/employee" method="POST" autocomplete="off">
<input type="hidden" name= "_id" value="{{employee._id}}">
    <div class="form-group">
        <lable>Full Name</lable>
        <input type="text" class="form-control" name="fullName" placeholder="Full Name" value="{{employee.city}}">
        <div class="text-danger">
            {{emplloyee.fullNameError}}</div>
     </div>
     <div class="form-group">
        <lable>Email</lable>
        <input type="text" class="form-control" name="fullName" placeholder="Email" value="{{employee.city}}">
         <div class="text-danger">
            {{emplloyee.emailError}}</div>
    </div>
    <div class="form-row">
        <div class="form-group col-md-6">
            <label>Mobile</label>
        <input type="text" class="form-control" name="fullName" placeholder="mobile" value="{{employee.city}}">
        </div>
   </div>
    <div class="form-group col-md-6">
            <label>City</label>
        <input type="text" class="form-control" name="fullName" placeholder="City" value="{{employee.city}}">
        </div>
   <div class="form-group">
       <button type="submit" class="btn btn-info"><i class="fa fa-database"></i>submit</button>
       <a class="btn btn-secondary" href="/employee/list"><i class="fa fa-plush"></i> View All </a>
   </div>
</form>